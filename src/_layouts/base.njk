<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="theme-color" content="#ff1277" />

        <title>cv | malte bär</title>

        <style>
            @font-face {
                font-family: 'DM Serif Display';
                src: url('/assets/fonts/dm-serif-display-v15-latin-regular.woff2')
                    format('woff2');
                font-weight: 400;
                font-display: swap;
                font-style: normal;
            }

            @font-face {
                font-family: 'DM Serif Display';
                src: url('/assets/fonts/dm-serif-display-v15-latin-italic.woff2')
                    format('woff2');
                font-weight: 400;
                font-display: swap;
                font-style: italic;
            }

            :is(h1, h2, h3, h4) {
                font-family: 'DM Serif Display', serif;
            }
        </style>

        <link rel="stylesheet" href="/assets/css/global.css" />
    </head>

    <body class="cover">
        {% include "partials/header.njk" %}

        <main class="wrapper">{{ content | safe }}</main>

        {% include "partials/footer.njk" %}
    </body>
</html>
