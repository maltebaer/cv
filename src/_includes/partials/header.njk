<header>
    <a href="/">
        {% include 'svg/code.svg' %}

        <span class="sr-only">home</span>
    </a>
    <nav>
        <ul>
            {% for item in navigation %}
                <li>
                    <a href="{{ item.href }}" {% if item.href == page.url %}aria-current="page"{% endif %}>{{ item.label }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
</header>
