<footer class="cluster" style="--cluster-space: var(--space-xs) var(--space-xl); --cluster-horizontal-alignment: space-between">
    <p>
        heavily inspired by
        <a
            href="https://buildexcellentwebsit.es"
            target="_blank"
            rel="noopener noreferrer"
            >buildexcellentwebsit.es
        </a>
    </p>

    <dl class="cluster" style="--cluster-space: var(--space-xs)">
        <dt>build with</dt>
        {% for tool in tools %}
            <dd>
                <a
                    class="will-move"
                    href="{{ tool.href }}"
                    target="_blank"
                    rel="noopener noreferrer">
                    {{ tool.title }}
                </a>
            </dd>
        {% endfor %}
    </dl>
</footer>
