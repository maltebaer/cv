---
order: 2
title: interests
---

<h2 id="{{ title }}">{{ title }}</h2>

<ul class="cluster">
    {% for interest in interests %}
        <li class="badge">{{ interest }}</li>
    {% endfor %}
</ul>
